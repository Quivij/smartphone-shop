$headers = @{
    "X-Forwarded-For" = "5.5.5.5"
}

for ($i = 0; $i -lt 100; $i++) {
    Invoke-WebRequest -Uri "http://localhost:3001/api/products/search?query=iphone" -Headers $headers
}

$headers = @{
    "X-Forwarded-For" = "1.2.3.4"
}
Invoke-WebRequest -Uri "http://localhost:3001/api/products/search?query=iphone" -Headers $headers